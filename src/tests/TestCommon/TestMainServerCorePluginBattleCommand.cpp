#include "gtest/gtest.h"
#include "gmock/gmock.h"
#include <boost/any.hpp>
#include <cstring>


TEST(TestMainServerCorePluginBattleCommand, test)
{
    /*std::string dsfsd = "velocity, 1;directionAngular, 1";

    // ParseStringTo::ParsedStrings params;
    // ParseStringTo::StringVector(dsfsd, ';', params);

    const char* const delimeters = ";";

    char* token = std::strtok(dsfsd.data(), delimeters);

    const char* const delimeters2 = ", ";
    char* token2;
    std::string str1 = token;
    std::vector<std::string> words;
    token2 = std::strtok(str1.data(), delimeters2);
    while (token2 != nullptr)
    {
            words.push_back(token2);
        token2 = std::strtok(nullptr, delimeters2);
    }*/



}




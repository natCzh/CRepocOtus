#ifndef _SCOPES_PROCESSING_H_
#define _SCOPES_PROCESSING_H_

#include <mutex>

class Ioc;

namespace Scopes
{
	class ScopesProcessing
	{
	public:
		/*static void processing(const std::string &key, IoC *curIoc)
		{
			if (strcmp(key.c_str(), "Scopes.New") == 0)
				int sdfsd = 0;


		}*/


	protected:

		/*size_t CreateScope(IoC *curIoc)
		{
			std::lock_guard<std::mutex> lock(mutex);
			curIoc.



		}*/
		
	};
}

#endif /* _SCOPES_PROCESSING_H_ */